# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[project]
name = "browsergym-web-resources-server"
version = "0.0.0"
requires-python = ">=3.13.14,<3.14"
dependencies = [
    "nemo-gym[dev]",
    "browsergym-webarena==0.14.3",
    # evaluate leaves datasets unconstrained. Pin a release compatible with
    # the current PyArrow surface used by the WebArena evaluator stack.
    "datasets==5.0.1",
    # Newer NLTK import-security hooks reject component-local imports when the
    # cluster's trusted egress proxy is present. Preserve benchmark-era NLTK.
    "nltk==3.8.1",
    "Pillow>=10",
]

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61"]

[tool.setuptools.packages.find]
where = ["../.."]
include = ["resources_servers.browsergym_web*"]

[tool.uv]
# BrowserGym 0.14.3 pins Playwright 1.44.0, whose greenlet 3.0.3 cannot build
# on Python 3.13. Greenlet 3.1.1 keeps the Playwright API contract and ships
# CPython 3.13 wheels. Gym's uv-pip setup path reads the matching overrides.txt.
override-dependencies = ["greenlet==3.1.1"]

[tool.uv.sources]
nemo-gym = { path = "../..", editable = true }
